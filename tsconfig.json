{
  // Root config for editor tooling and root-level files (e.g. eslint.config.js).
  // Each project has its own tsconfig.json that extends ./tsconfig.base.json.
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "allowJs": true
  },
  "include": ["eslint.config.js"]
}
